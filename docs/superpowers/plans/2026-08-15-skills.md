# Skills(按需加载的技能目录)Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在系统提示末尾追加技能目录(skill catalog),模型用 `read` 工具按目录给出的路径读取技能全文 —— 全文不注入提示。

**Architecture:** 新模块 `gasket-host/src/skills.rs` 提供 `append_skills(base, cwd)`:扫描全局 `~/.gasket/skills/*.md` 与项目 `<cwd>/.gasket/skills/*.md`(同名项目胜),手解析 frontmatter(`name:` + `description:`,缺任一则 `tracing::warn` 跳过),把目录行拼到 base 之后。三个 host 调用点(CLI / gateway ws / 桌面 chat.rs)在构建 base 提示字符串处各改一行。零新依赖。

**Tech Stack:** std::fs + std::path(手解析 frontmatter)、tracing、tempfile(dev,workspace 已有)。

## Global Constraints

- 扫描顺序:先全局 `gasket_core::storage::config_dir()/skills/*.md`,后项目 `<cwd>/.gasket/skills/*.md`;frontmatter `name:` 相同时项目覆盖全局。
- frontmatter 契约:文件以 `---\n` 开头,至下一个 `---` 行结束;块内须同时含 `name:` 与 `description:` 前缀行,否则 `tracing::warn!` 后跳过。
- 只注入目录行 + 一条 read 说明,**不注入正文**;无有效技能时原样返回 base。
- 全局技能给绝对路径 —— 依赖 `read` 现有规则(绝对路径仅允许 `~/.gasket` 内,`gasket-core/src/tools/mod.rs` `resolve_read_path` ~line 132);项目技能路径在 cwd 内,相对路径即可。
- subagent 提示不做技能增强(超出范围,`HostSubagentSpawner` 不动)。
- rustfmt:4 空格、max_width 100;CI:`cargo fmt --check`、`cargo clippy --all-features --all-targets -D warnings`、`cargo test --all-features`。

---

### Task 1: `skills.rs` 扫描 + frontmatter 解析(纯函数 + tempfile 测试)

**Files:**
- Create: `gasket/gasket-host/src/skills.rs`
- Modify: `gasket/gasket-host/src/lib.rs`(模块区 ~line 13-24 字母序插 `pub mod skills;`;re-export 区加 `pub use skills::append_skills;`)
- Test: `gasket/gasket-host/src/skills.rs` 内 `#[cfg(test)] mod tests`

**Interfaces:**
- Produces: `pub fn append_skills(base: &str, cwd: &Path) -> String`(生产入口,全局根 = `gasket_core::storage::config_dir()`)
- Produces: `pub fn append_skills_in(base: &str, cwd: &Path, global_root: &Path) -> String`(可测内核,同 `resolve_read_path_in` 注入模式)

- [ ] **Step 1: Write the failing test**

```rust
#[cfg(test)]
mod tests {
    use super::*;
    use std::fs;
    use std::path::Path;

    fn skill_file(dir: &Path, file: &str, body: &str) {
        fs::create_dir_all(dir.join("skills")).unwrap();
        fs::write(dir.join("skills").join(file), body).unwrap();
    }

    const OK: &str = "---\nname: code-review\ndescription: Review diffs for bugs\n---\nBody never injected.\n";

    #[test]
    fn appends_catalog_for_valid_skill() {
        let tmp = tempfile::tempdir().unwrap();
        skill_file(tmp.path(), "review.md", OK);
        let out = append_skills_in("BASE", Path::new("/nope"), tmp.path());
        assert!(out.starts_with("BASE"));
        assert!(out.contains("## Skills"));
        assert!(out.contains("- name: code-review — Review diffs for bugs"));
        assert!(out.contains(&format!("(source: {})", tmp.path().join("skills/review.md").display())));
        assert!(out.contains("`read`"), "must tell the model to use the read tool");
        assert!(!out.contains("Body never injected"));
    }

    #[test]
    fn missing_description_and_no_frontmatter_are_skipped() {
        let tmp = tempfile::tempdir().unwrap();
        skill_file(tmp.path(), "bad.md", "---\nname: x\n---\nbody\n");
        assert_eq!(append_skills_in("BASE", Path::new("/nope"), tmp.path()), "BASE");
        skill_file(tmp.path(), "plain.md", "Just some markdown.\n");
        assert_eq!(append_skills_in("BASE", Path::new("/nope"), tmp.path()), "BASE");
    }

    #[test]
    fn no_skills_returns_base_unchanged() {
        let tmp = tempfile::tempdir().unwrap();
        assert_eq!(append_skills_in("BASE", Path::new("/nope"), tmp.path()), "BASE");
    }

    #[test]
    fn project_overrides_global_on_name_clash() {
        let g = tempfile::tempdir().unwrap();
        let p = tempfile::tempdir().unwrap();
        skill_file(g.path(), "a.md", "---\nname: dup\ndescription: global one\n---\n");
        skill_file(p.path(), "b.md", "---\nname: dup\ndescription: project one\n---\n");
        let out = append_skills_in("BASE", p.path(), g.path());
        assert!(out.contains("project one") && !out.contains("global one"));
        assert_eq!(out.matches("- name: dup").count(), 1, "exactly one line per name");
    }

    #[test]
    fn project_only_skill_is_included() {
        let g = tempfile::tempdir().unwrap();
        let p = tempfile::tempdir().unwrap();
        skill_file(p.path(), "only.md", "---\nname: proj\ndescription: from project\n---\n");
        assert!(append_skills_in("BASE", p.path(), g.path()).contains("- name: proj — from project"));
    }
}
```

- [ ] **Step 2: Run to verify it fails**

```bash
cd /Users/yeheng/workspaces/Github/gasket/gasket && cargo test -p gasket-host skills
# expected: compile error — append_skills / append_skills_in 未定义
```

- [ ] **Step 3: Minimal implementation**

`gasket/gasket-host/src/skills.rs`:

```rust
//! Skills: a prompt-appended catalog of on-disk instruction files.
//!
//! Global skills live at `<config_dir>/skills/*.md`; project skills at
//! `<cwd>/.gasket/skills/*.md` (same `name:` wins over the global one).
//! Only the catalog line + absolute path is appended — the model reads a
//! skill's full content with the `read` tool on demand.

use std::collections::BTreeMap;
use std::path::{Path, PathBuf};

struct SkillMeta {
    description: String,
    source: PathBuf,
}

/// Production entry: global root is gasket's config dir (`~/.gasket`).
pub fn append_skills(base: &str, cwd: &Path) -> String {
    append_skills_in(base, cwd, &gasket_core::storage::config_dir())
}

/// Testable core: `global_root` is injected (production uses the config dir).
pub fn append_skills_in(base: &str, cwd: &Path, global_root: &Path) -> String {
    let mut catalog: BTreeMap<String, SkillMeta> = BTreeMap::new();
    scan_dir(&global_root.join("skills"), &mut catalog);
    scan_dir(&cwd.join(".gasket").join("skills"), &mut catalog);
    if catalog.is_empty() {
        return base.to_string();
    }
    let mut out = String::from(base);
    out.push_str(
        "\n\n## Skills\n\nThe entries below are skills. To follow one, load its \
         full instructions with the `read` tool using the path shown for it.\n\n",
    );
    for (name, meta) in &catalog {
        out.push_str(&format!(
            "- name: {name} — {} (source: {})\n",
            meta.description,
            meta.source.display()
        ));
    }
    out
}

fn scan_dir(dir: &Path, catalog: &mut BTreeMap<String, SkillMeta>) {
    let entries = match std::fs::read_dir(dir) {
        Ok(e) => e,
        Err(_) => return, // no dir = no skills; not an error
    };
    for entry in entries.flatten() {
        let path = entry.path();
        if path.extension().and_then(|e| e.to_str()) != Some("md") {
            continue;
        }
        match std::fs::read_to_string(&path).ok().and_then(|c| parse_frontmatter(&c)) {
            Some((name, description)) => {
                catalog.insert(name, SkillMeta { description, source: path });
            }
            None => tracing::warn!(path = %path.display(), "skill missing frontmatter name/description; skipped"),
        }
    }
}

/// Hand-parsed frontmatter: `---\n` … `---` block carrying both `name:` and
/// `description:` lines. Anything else is None (caller warns + skips).
fn parse_frontmatter(content: &str) -> Option<(String, String)> {
    let mut name = None;
    let mut description = None;
    for line in content.strip_prefix("---\n")?.lines() {
        if line.trim_end() == "---" {
            return match (name, description) {
                (Some(n), Some(d)) => Some((n, d)),
                _ => None,
            };
        }
        if let Some(v) = line.strip_prefix("name:") {
            name = Some(v.trim().to_string());
        } else if let Some(v) = line.strip_prefix("description:") {
            description = Some(v.trim().to_string());
        }
    }
    None // unterminated frontmatter block
}
```

`lib.rs` 模块区插入 `pub mod skills;`,re-export 区加 `pub use skills::append_skills;`。

- [ ] **Step 4: Run to verify it passes**

```bash
cd /Users/yeheng/workspaces/Github/gasket/gasket && cargo test -p gasket-host skills
# expected: 5 passed
cargo fmt -p gasket-host && cargo clippy -p gasket-host --all-targets -D warnings
# expected: clean
```

- [ ] **Step 5: Commit**

```bash
git add gasket/gasket-host/src/skills.rs gasket/gasket-host/src/lib.rs
git commit -m "feat(host): skills catalog appended to system prompt"
```

---

### Task 2: 三个 host 调用点接线

**Files:**
- Modify: `gasket/gasket-cli/src/main.rs`(~line 89,`Host::new` 第 4 参,当前字面量 `"You are a helpful, concise assistant.".to_string()`)
- Modify: `gasket/gasket-gateway/src/ws.rs`(~line 133,`let system_prompt = "You are a helpful, concise assistant.".to_string();`)
- Modify: `web/src-tauri/src/chat.rs`(~line 251,`build_session` 内同一字面量)

**Interfaces:**
- Consumes: `gasket_host::append_skills`(Task 1)。cwd 取法与 `Host::new` 内部一致(`std::env::current_dir().unwrap_or_else(|_| PathBuf::from("."))`,`gasket-host/src/lib.rs` ~line 96)——提示构建早于 `Host::new`,需在调用点自取。

- [ ] **Step 1: Write the failing test**

三个调用点均为内联字面量,无单测缝;用可机检的 grep 断言作失败条件:

```bash
cd /Users/yeheng/workspaces/Github/gasket
grep -rn 'append_skills(' gasket/gasket-cli/src/main.rs gasket/gasket-gateway/src/ws.rs web/src-tauri/src/chat.rs | wc -l
```

- [ ] **Step 2: Run to verify it fails**

```bash
# (同上命令)expected: 0
```

- [ ] **Step 3: Minimal implementation**

CLI `main.rs`(~line 85-92,`Host::new` 前):

```rust
    let cwd = std::env::current_dir().unwrap_or_else(|_| std::path::PathBuf::from("."));
    let mut host = Host::new(
        host_cfg,
        session,
        policy.clone(),
        gasket_host::append_skills("You are a helpful, concise assistant.", &cwd),
        assemble_tools(&ext_tools, &extra_tools, &mcp_tools),
    )
```

gateway `ws.rs`(~line 133 替换该行):

```rust
    let cwd = std::env::current_dir().unwrap_or_else(|_| std::path::PathBuf::from("."));
    let system_prompt = gasket_host::append_skills("You are a helpful, concise assistant.", &cwd);
```

桌面端 `chat.rs`(~line 251 替换该行;该文件为 2 空格缩进,遵循原风格):

```rust
  let cwd = std::env::current_dir().unwrap_or_else(|_| std::path::PathBuf::from("."));
  let system_prompt = gasket_host::append_skills("You are a helpful, concise assistant.", &cwd);
```

- [ ] **Step 4: Run to verify it passes**

```bash
grep -rn 'append_skills(' gasket/gasket-cli/src/main.rs gasket/gasket-gateway/src/ws.rs web/src-tauri/src/chat.rs | wc -l
# expected: 3
cd /Users/yeheng/workspaces/Github/gasket/gasket && cargo build -p gasket-cli -p gasket-gateway
cd /Users/yeheng/workspaces/Github/gasket/web/src-tauri && cargo build
# expected: 均成功
```

- [ ] **Step 5: Commit**

```bash
git add gasket/gasket-cli/src/main.rs gasket/gasket-gateway/src/ws.rs web/src-tauri/src/chat.rs
git commit -m "feat(host): wire skills catalog into CLI, gateway, and desktop prompts"
```

---

### Task 3: 文档(usage.md 新小节 + 示例技能文件)

**Files:**
- Modify: `docs/usage.md`(§9.5 权限模式之后新增 `### 9.6 Skills(可选)`)

**Interfaces:** 无 —— 纯文档。

- [ ] **Step 1: Write the failing test**

```bash
grep -c '### 9.6 Skills' /Users/yeheng/workspaces/Github/gasket/docs/usage.md   # expected: 0
```

- [ ] **Step 2: Run to verify it fails**

```bash
# (同上)expected: 0
```

- [ ] **Step 3: Minimal implementation**

§9.5 之后插入:

````markdown
### 9.6 Skills(可选)

技能是磁盘上的 Markdown 指令文件。启动时 gasket 只把「名称 + 描述 + 文件路径」目录追加到系统提示;模型需要某个技能时,用 `read` 工具按目录给出的路径读取全文。全文不进入系统提示,不占用上下文预算。子代理(subagent)提示不参与技能目录。

两个存放位置(同名时项目覆盖全局):

- 全局:`~/.gasket/skills/*.md`(绝对路径,`read` 已允许读取 `~/.gasket` 内的绝对路径)
- 项目:`<项目根>/.gasket/skills/*.md`

文件必须以 YAML frontmatter 开头,`name` 与 `description` 缺一不可,否则该文件被跳过并在日志告警。示例 `~/.gasket/skills/commit-style.md`:

```markdown
---
name: commit-style
description: Enforce conventional commit messages with scope
---
Write commit titles as `type(scope): summary`, lowercase, imperative mood...
```
````

- [ ] **Step 4: Run to verify it passes**

```bash
grep -c '### 9.6 Skills' /Users/yeheng/workspaces/Github/gasket/docs/usage.md   # expected: 1
```

- [ ] **Step 5: Commit**

```bash
git add docs/usage.md && git commit -m "docs: skills catalog usage and example skill file"
```
